// Shared proxy base URL for all authenticated API calls.
// Used in: authService.ts, proxyService.ts, and SubscriptionGate.tsx (component scope)
export const PROXY_BASE = 'https://voicetype-web.vercel.app'
